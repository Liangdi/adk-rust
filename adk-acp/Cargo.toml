[package]
name = "agentx-adk-acp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
documentation = "https://docs.rs/adk-acp"
readme = "README.md"
description = "Agent Client Protocol integration for ADK-Rust — use ACP coding agents or expose an ADK agent to editors"
keywords = ["adk", "acp", "agent", "protocol", "ai"]
categories = ["development-tools", "asynchronous"]

[features]
default = []
server = ["dep:adk-runner", "dep:adk-session", "dep:adk-tool", "dep:rmcp", "dep:tokio-util", "dep:uuid", "dep:async-stream"]
http = ["dep:agent-client-protocol-http", "dep:axum", "server"]
# Accept `McpServer::Http` entries in `session/new` (streamable HTTP MCP clients
# via adk-tool's McpHttpClientBuilder) and advertise `mcp_capabilities.http`.
mcp-http = ["server", "adk-tool?/http-transport"]

[dependencies]
adk-core = { workspace = true }
# `unstable_session_fork` unlocks the ACP `session/fork` types and JSON-RPC
# dispatch (`ForkSessionRequest` / `ForkSessionResponse`, the `session.fork`
# capability). The method is stable-adjacent in the SDK but gated behind this
# feature; we enable it to implement Requirement 10 (session fork).
agent-client-protocol = { version = "2.0.0", features = ["unstable_session_fork", "unstable_elicitation"] }
async-trait = { workspace = true }
base64 = "0.22"
futures = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["sync", "process", "io-util", "io-std", "macros", "rt", "time", "net"] }
tracing = { workspace = true }

# Server-specific dependencies (behind `server` feature)
adk-runner = { workspace = true, optional = true }
adk-session = { workspace = true, optional = true }
adk-tool = { workspace = true, features = ["mcp"], optional = true }
rmcp = { version = "3.1", features = ["client", "transport-child-process"], optional = true }
tokio-util = { version = "0.7", features = ["rt"], optional = true }
uuid = { workspace = true, optional = true }
async-stream = { workspace = true, optional = true }

# HTTP transport dependencies (behind `http` feature, which implies `server`)
agent-client-protocol-http = { version = "2.0.0", features = ["server"], optional = true }
axum = { version = "0.8", optional = true }

[dev-dependencies]
# Internal dev-deps are path-only (no version) so cargo strips them from the
# published manifest — they can never block or reorder a crates.io publish.
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "test-util"] }
proptest = "1.5"
adk-session = { package = "agentx-adk-session", path = "../adk-session" }
adk-runner = { package = "agentx-adk-runner", path = "../adk-runner" }
